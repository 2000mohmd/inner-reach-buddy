# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Playwright (installed on-demand, not tracked; see e2e/README.md)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Lock files — bun.lock is the single source of truth (Lovable's platform
# package manager). Do not commit npm/pnpm/yarn lockfiles alongside it.
package-lock.json
pnpm-lock.yaml
yarn.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
